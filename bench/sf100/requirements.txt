# Pinned for bench/sf100 lakehouse tooling (build-lakehouse.py + local rehearsal).
# Versions resolved from PyPI on 2026-07-26 — do not invent bumps from memory.
#
# boto3 is constrained to keep botocore compatible with s3fs→aiobotocore 3.8.x
# (aiobotocore 3.8.0 wants botocore>=1.43.3,<1.43.47).
pyiceberg[pyarrow,s3fs]==0.11.1
deltalake==1.6.2
pyarrow==25.0.0
SQLAlchemy==2.0.51
s3fs==2026.6.0
fsspec==2026.6.0
boto3==1.43.46
